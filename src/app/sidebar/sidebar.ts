import { Component } from '@angular/core';

/** Stub — fleshed out in its own dedicated commit (avatar, availability, stack chips,
 *  location/local-time, GitHub activity, social row). Exists now just so the app shell
 *  (which renders <app-sidebar /> unconditionally) has something real to compile against. */
@Component({
  selector: 'app-sidebar',
  template: `<aside class="sidebar"><p>Sidebar — pendiente</p></aside>`,
})
export class SidebarComponent {}
