import { Component } from '@angular/core';

/** Stub — real content (timeline, languages, stack) wired up in its own dedicated commit. */
@Component({
  selector: 'app-resume-panel',
  template: `<div class="panel"><p>Resume — pendiente</p></div>`,
})
export class ResumePanelComponent {}
