import { Component } from '@angular/core';

/** Stub — real CV document rendering wired up in its own dedicated commit. */
@Component({
  selector: 'app-cv-panel',
  template: `<div class="panel"><p>CV — pendiente</p></div>`,
})
export class CvPanelComponent {}
