import { Component } from '@angular/core';

/** Stub — real project grid + detail view wired up in its own dedicated commit. */
@Component({
  selector: 'app-portfolio-panel',
  template: `<div class="panel"><p>Portfolio — pendiente</p></div>`,
})
export class PortfolioPanelComponent {}
